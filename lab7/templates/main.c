//#include <config.h>
// I don't know why, but the <config.h> won't be compired but the down 
//formet is ok
#include "config.h"
//#include <sensor_db.c>
#include "sensor_db.c"

int main(int argc, char const *argv[])
{
	/* code */
	return 0;
}